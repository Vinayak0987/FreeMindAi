version: '3.8'

services:
  test-music-classifier:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ENVIRONMENT=production
      - PORT=5000
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
